{
  "name": "sat-math-app",
  "version": "1.0.0",
  "description": "SAT Math Practice Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd SAT2 && npm install && npm run build",
    "dev": "nodemon server.js",
    "postinstall": "cd SAT2 && npm install"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.12",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "bcryptjs": "^2.4.3",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "18.x"
  }
}
