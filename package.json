{
  "name": "sat-math-app",
  "version": "1.0.0",
  "description": "SAT Math Practice Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd SAT2 && NODE_OPTIONS='--openssl-legacy-provider' npm install && npm run build",
    "dev": "nodemon server.js",
    "postinstall": "cd SAT2 && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongodb": "^6.14.2",
    "mongoose": "^6.0.12",
    "path": "^0.12.7"
  },
  "engines": {
    "node": "16.x"
  }
}
